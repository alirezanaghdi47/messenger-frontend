import {createSlice} from '@reduxjs/toolkit';

const initialState = {

}

export const chat = createSlice({
    name: 'chat',
    initialState,
    reducers: {

    },
})

export const {

} = chat.actions;

export default chat.reducer;